# PLP MongoDB Assignment

## Setup

1. Clone this repository
2. Install dependencies: `npm install mongodb`
3. Update your MongoDB Atlas connection string in `insert_books.js` and `queries.js`
4. Run `insert_books.js` to insert books (if not already in Atlas):
   ```bash
   node insert_books.js
